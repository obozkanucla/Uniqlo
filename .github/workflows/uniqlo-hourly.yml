name: Uniqlo 15 MIN scrapers, event detector & notifier

on:
  schedule:
    - cron: "0,20,40 10-21 * * *"
  workflow_dispatch:

concurrency:
  group: uniqlo-scrape
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # --------------------------------------------------
      # Playwright
      # --------------------------------------------------
      - name: Install Playwright browsers
        run: |
          python -m playwright install chromium --with-deps

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3 libatk1.0-0 libx11-xcb1

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ---------------------------------------------
      # Restore SQLite DB
      # ---------------------------------------------
      - name: Restore SQLite database
        run: |
          set -e
          mkdir -p db
          gh release download uniqlo-db-latest \
            --pattern uniqlo.sqlite \
            --output db/uniqlo.sqlite \
            --clobber || echo "No existing DB found — starting fresh"
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ---------------------------------------------
      # Scrape
      # ---------------------------------------------
      - name: Run catalog scrapers
        run: python src/scrape_catalog.py

      - name: Run availability scrapers
        run: python src/scrape_availability.py

      # ---------------------------------------------
      # Detect events
      # ---------------------------------------------
      - name: Detect events
        env:
          PYTHONPATH: .
        run: |
          python src/detect_events.py

      # ---------------------------------------------
      # Notify
      # ---------------------------------------------
      - name: Notify events
        continue-on-error: true
        env:
          PYTHONPATH: .
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID_BURAK: ${{ secrets.TELEGRAM_CHAT_ID_BURAK }}
          TELEGRAM_CHAT_ID_MUGE: ${{ secrets.TELEGRAM_CHAT_ID_MUGE }}
        run: |
          python src/notify_events.py

      # ---------------------------------------------
      # Persist DB
      # ---------------------------------------------
      - name: Publish SQLite snapshot
        uses: softprops/action-gh-release@v2
        with:
          tag_name: uniqlo-db-latest
          name: Uniqlo Sale SQLite (Latest)
          files: db/uniqlo.sqlite
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}